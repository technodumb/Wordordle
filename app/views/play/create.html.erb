<div class="bg-gray-900 w-3/5 h-4/5 rounded-xl p-3 min-w-[500px] max-w-[750px]">
    <% form_with url:'generate_room' do |f| %>
        <div class="flex flex-col gap-3">
            <div class="flex flex-col gap-2">
                <%= f.label :room_name, class: "text-white" %>
                <%= f.text_field :room_name, class: "bg-black text-white rounded-xl p-2" %>
            </div>
            <div class="flex flex-col gap-2">
                <%= f.label :room_password, class: "text-white" %>
                <%= f.password_field :room_password, class: "bg-black text-white rounded-xl p-2" %>
            </div>
            <div class="flex flex-col gap-2">
                <%= f.label :room_password_confirmation, class: "text-white" %>
                <%= f.password_field :room_password_confirmation, class: "bg-black text-white rounded-xl p-2" %>
            </div>
            <div class="flex justify-center">
                <%= f.submit 'Create', class: "bg-green-
</div>
